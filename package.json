{
  "name": "clawdex",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
